# Vite local development usually does not need this file.
# The dev server proxies /api to http://127.0.0.1:8000.
#
# Set this only when serving the frontend without the Vite proxy.
VITE_API_BASE_URL=/api/v1
